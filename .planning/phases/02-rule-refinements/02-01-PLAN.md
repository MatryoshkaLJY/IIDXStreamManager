---
phase: 2
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - iidx_knockout_scoreboard/index.html
  - iidx_knockout_scoreboard/style.css
  - iidx_knockout_scoreboard/app.js
autonomous: true
requirements:
  - UI-01
  - SCOR-01
  - CMD-01
must_haves:
  truths:
    - Champion trophy code fully removed from HTML, CSS, and JS
    - player-score displays cumulative raw scores across rounds
    - INIT command preserves .title-text and .stage-indicator DOM structure
  artifacts:
    - path: "iidx_knockout_scoreboard/index.html"
      provides: "Removal of champion trophy markup"
    - path: "iidx_knockout_scoreboard/style.css"
      provides: "Removal of champion trophy styles"
    - path: "iidx_knockout_scoreboard/app.js"
      provides: "Cumulative scoring and init title preservation logic"
  key_links:
    - from: "app.js handleInit/handleReset"
      to: "index.html .title-text element"
      via: "textContent update preserving DOM children"
---

# Phase 2 Plan 01: Cleanup and Foundation Fixes

## Objective
Remove champion trophy code completely, fix init command to preserve header structure, and implement cumulative raw score tracking per round.

<task>
  <name>Remove Champion Trophy from HTML, CSS, and JS</name>
  <type>auto</type>
  <files>
    - iidx_knockout_scoreboard/index.html
    - iidx_knockout_scoreboard/style.css
    - iidx_knockout_scoreboard/app.js
  </files>
  <read_first>
    - iidx_knockout_scoreboard/index.html
    - iidx_knockout_scoreboard/style.css
    - iidx_knockout_scoreboard/app.js
  </read_first>
  <action>
    1. In index.html, delete the commented-out `champion-trophy` div completely.
    2. In style.css, delete the commented-out `.champion-trophy`, `.trophy-icon`, `.champion-label`, `.champion-name`, and `@keyframes trophyFloat` / `@keyframes championAura` blocks completely.
    3. In app.js, delete the `clearChampionDisplay()` method and the `lightChampionPath(champion)` method entirely. Remove all calls to `clearChampionDisplay()` and `lightChampionPath()` from `handleReset()`, `handleInit()`, and `handleSettle()`.
  </action>
  <verify>
    - `grep -n "champion-trophy\|champion-name\|trophy-icon\|champion-label" iidx_knockout_scoreboard/index.html` returns no matches
    - `grep -n "champion-trophy\|champion-name\|trophy-icon\|champion-label\|trophyFloat\|championAura" iidx_knockout_scoreboard/style.css` returns no matches
    - `grep -n "clearChampionDisplay\|lightChampionPath\|champion-name" iidx_knockout_scoreboard/app.js` returns no matches
  </verify>
  <done>
    Champion trophy code fully removed and grep-verified in all three files.
  </done>
</task>

<task>
  <name>Fix init Command to Preserve Header Structure</name>
  <type>auto</type>
  <files>
    - iidx_knockout_scoreboard/app.js
  </files>
  <read_first>
    - iidx_knockout_scoreboard/app.js
    - iidx_knockout_scoreboard/index.html
  </read_first>
  <action>
    In app.js, modify the tournament title update logic in `handleInit()` and `handleReset()`:
    - In `handleInit()`, change `const titleEl = document.querySelector('.tournament-title'); if (titleEl) titleEl.textContent = data.tournamentName;` to `const titleTextEl = document.querySelector('.title-text'); if (titleTextEl) titleTextEl.textContent = data.tournamentName;`
    - In `handleReset()`, change the title reset from `titleEl.textContent = '淘汰赛';` to `document.querySelector('.title-text').textContent = '16人淘汰赛';`
  </action>
  <verify>
    - `handleInit` contains `document.querySelector('.title-text')` and sets its `textContent`
    - `handleReset` contains `document.querySelector('.title-text')` and sets `textContent` to `'16人淘汰赛'`
    - No `tournament-title.textContent` assignment remains in `handleInit` or `handleReset`
  </verify>
  <done>
    Init and reset commands now update only .title-text, preserving .stage-indicator.
  </done>
</task>

<task>
  <name>Make player-score Cumulative Across Rounds</name>
  <type>auto</type>
  <files>
    - iidx_knockout_scoreboard/app.js
  </files>
  <read_first>
    - iidx_knockout_scoreboard/app.js
  </read_first>
  <action>
    In app.js `handleScore()`, locate the `updatePlayerNode` call inside the scores loop and change:
    `score: rawScore,` → `score: player.totalRawScore,`
  </action>
  <verify>
    - Inside `handleScore`, the `updatePlayerNode` call for score uses `score: player.totalRawScore` instead of `score: rawScore`
  </verify>
  <done>
    Player nodes now display cumulative raw score instead of single-round score.
  </done>
</task>

---
phase: 04-tournament-setup-round-prep-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/app.py
  - src/templates/round_prep.html
  - static/css/main.css
autonomous: true
requirements:
  - UI-03
  - UI-04
  - AUTO-05
must_haves:
  truths:
    - "Operator can visit /round_prep to see current round and cabinet assignment dropdowns"
    - "Previous/Next buttons change the round number with bounds checking"
    - "Saving round persists cabinet assignments to RuntimeState"
    - "Dropdown options are populated from the loaded config's player list"
  artifacts:
    - path: "src/templates/round_prep.html"
      provides: "Round prep page UI"
    - path: "src/app.py"
      provides: "/round_prep, /save_round, /change_round routes"
  key_links:
    - from: "/round_prep GET"
      to: "src/config/loader.py"
      via: "get_player_names for dropdown options"
      pattern: "get_player_names\\(loaded, runtime_state\\.mode\\)"
    - from: "/save_round POST"
      to: "src/state.py"
      via: "cabinet_assignments update"
      pattern: "runtime_state\\.cabinet_assignments\\s*="
---

<objective>
Build the Round Prep page with round navigation and cabinet assignment dropdowns that persist to RuntimeState.

Purpose: Enable the operator to assign players to IIDX#1~IIDX#4 cabinets for each round.
Output: Updated src/app.py with round prep routes, new round_prep.html template, and CSS additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-tournament-setup-round-prep-ui/04-CONTEXT.md
@.planning/phases/04-tournament-setup-round-prep-ui/04-PATTERNS.md
@.planning/phases/04-tournament-setup-round-prep-ui/04-UI-SPEC.md
@src/app.py
@src/state.py
@src/config/loader.py
@src/templates/base.html
@static/css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add round prep routes and forms to Flask app</name>
  <files>src/app.py</files>
  <read_first>src/app.py, src/state.py, src/config/loader.py</read_first>
  <action>
Add the following to src/app.py inside `create_app`:

1. Add `RoundPrepForm` class after `ModeForm`:
```python
class RoundPrepForm(BaseModel):
    cabinet_1: str = "Unassigned"
    cabinet_2: str = "Unassigned"
    cabinet_3: str = "Unassigned"
    cabinet_4: str = "Unassigned"
```

2. Add `/round_prep` GET route:
```python
@app.route("/round_prep")
def round_prep():
    runtime_state = load_runtime_state()
    player_names = []
    config_error = app.config.get("CONFIG_ERROR")
    if runtime_state.mode and not config_error:
        try:
            loaded = load_configs()
            player_names = get_player_names(loaded, runtime_state.mode)
        except ConfigError:
            pass
    return render_template(
        "round_prep.html",
        config_error=config_error,
        runtime_state=runtime_state,
        player_names=player_names,
    )
```

3. Add `/save_round` POST route:
```python
@app.route("/save_round", methods=["POST"])
def save_round():
    payload = request.get_json(silent=True) or request.form.to_dict()
    try:
        form = RoundPrepForm(**payload)
    except Exception as exc:
        return jsonify({"success": False, "error": str(exc)}), 200

    runtime_state = load_runtime_state()
    runtime_state.cabinet_assignments = {
        "IIDX#1": form.cabinet_1,
        "IIDX#2": form.cabinet_2,
        "IIDX#3": form.cabinet_3,
        "IIDX#4": form.cabinet_4,
    }
    save_runtime_state(runtime_state)
    socketio.emit(
        "round_saved",
        {
            "round": runtime_state.current_round,
            "assignments": runtime_state.cabinet_assignments,
        },
    )
    return jsonify({"success": True}), 200
```

4. Add `/change_round` POST route:
```python
@app.route("/change_round", methods=["POST"])
def change_round():
    payload = request.get_json(silent=True) or request.form.to_dict()
    direction = (payload.get("direction") or "").strip()
    if direction not in ("prev", "next"):
        return jsonify({"success": False, "error": "Invalid direction"}), 200

    runtime_state = load_runtime_state()
    if direction == "prev":
        runtime_state.current_round = max(1, runtime_state.current_round - 1)
    else:
        runtime_state.current_round = runtime_state.current_round + 1
    save_runtime_state(runtime_state)
    socketio.emit("round_changed", {"round": runtime_state.current_round})
    return jsonify({"success": True, "round": runtime_state.current_round}), 200
```
  </action>
  <verify>
    <automated>python -c "from src.app import create_app; app, _ = create_app(return_socketio=True); print([r.rule for r in app.url_map.iter_rules() if r.rule in ('/round_prep', '/save_round', '/change_round')])"</automated>
  </verify>
  <done>/round_prep, /save_round, and /change_round routes exist and handle round display, cabinet assignment persistence, and round navigation with bounds checking.</done>
  <acceptance_criteria>
    - src/app.py contains `class RoundPrepForm(BaseModel):`
    - src/app.py contains `@app.route("/round_prep")`
    - src/app.py contains `@app.route("/save_round", methods=["POST"])`
    - src/app.py contains `@app.route("/change_round", methods=["POST"])`
    - /change_round handler contains `max(1, runtime_state.current_round - 1)`
    - /save_round handler updates `runtime_state.cabinet_assignments` with keys IIDX#1 through IIDX#4
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create round_prep.html template</name>
  <files>src/templates/round_prep.html</files>
  <read_first>src/templates/base.html, src/templates/config.html</read_first>
  <action>
Create src/templates/round_prep.html extending base.html with the following content:

```html
{% extends "base.html" %}
{% block title %}Round Prep — IIDX Tournament Auto-Director{% endblock %}
{% block content %}
<div class="card">
  <h1 class="display">Round Prep</h1>
  <p class="label">Mode: <span class="status-ok">{{ runtime_state.mode or "Not set" }}</span></p>
  <p class="display">Round {{ runtime_state.current_round }}</p>
</div>

{% if config_error %}
<div class="card">
  <p class="status-error">{{ config_error }}</p>
  <p class="label">No config loaded</p>
  <p>Upload a tournament JSON config to begin.</p>
</div>
{% else %}
<div class="card">
  <h2 class="heading">Assign Players to Cabinets</h2>
  <form id="round-form" method="post" action="{{ url_for('save_round') }}">
    <div class="assignment-grid">
      {% for cabinet in ["IIDX#1", "IIDX#2", "IIDX#3", "IIDX#4"] %}
      <div class="assignment-card">
        <label class="label" for="{{ cabinet }}">{{ cabinet }}</label>
        <select id="{{ cabinet }}" name="cabinet_{{ loop.index }}" class="cabinet-select">
          <option value="Unassigned" {% if runtime_state.cabinet_assignments.get(cabinet, 'Unassigned') == 'Unassigned' %}selected{% endif %}>Unassigned</option>
          {% for player in player_names %}
          <option value="{{ player }}" {% if runtime_state.cabinet_assignments.get(cabinet, 'Unassigned') == player %}selected{% endif %}>{{ player }}</option>
          {% endfor %}
        </select>
      </div>
      {% endfor %}
    </div>
    <div class="button-row" style="margin-top: var(--space-md);">
      <button type="submit">Save Round</button>
    </div>
  </form>
</div>

<div class="card">
  <h2 class="heading">Round Navigation</h2>
  <div class="button-row">
    <form method="post" action="{{ url_for('change_round') }}">
      <input type="hidden" name="direction" value="prev">
      <button type="submit" {% if runtime_state.current_round <= 1 %}disabled{% endif %}>Previous Round</button>
    </form>
    <form method="post" action="{{ url_for('change_round') }}">
      <input type="hidden" name="direction" value="next">
      <button type="submit">Next Round</button>
    </form>
  </div>
</div>
{% endif %}
{% endblock %}
```
  </action>
  <verify>
    <automated>test -f src/templates/round_prep.html && grep -q "Round Prep" src/templates/round_prep.html && grep -q "IIDX#1" src/templates/round_prep.html && grep -q "Save Round" src/templates/round_prep.html</automated>
  </verify>
  <done>round_prep.html exists, extends base.html, displays round number, mode, cabinet assignment dropdowns, and round navigation buttons.</done>
  <acceptance_criteria>
    - src/templates/round_prep.html contains `{% extends "base.html" %}`
    - src/templates/round_prep.html contains `Round {{ runtime_state.current_round }}`
    - src/templates/round_prep.html contains dropdowns for IIDX#1, IIDX#2, IIDX#3, IIDX#4
    - src/templates/round_prep.html contains `Unassigned` as the first option in each dropdown
    - src/templates/round_prep.html contains `Previous Round` button with `disabled` when round <= 1
    - src/templates/round_prep.html contains `Next Round` button
    - src/templates/round_prep.html contains `Save Round` button
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add CSS for assignment grid and selects</name>
  <files>static/css/main.css</files>
  <read_first>static/css/main.css</read_first>
  <action>
Append the following CSS to the end of static/css/main.css:

```css
.assignment-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--space-md);
  margin-top: var(--space-md);
}

.assignment-card {
  background: var(--color-dominant);
  border-radius: 4px;
  padding: var(--space-md);
}

.assignment-card .label {
  display: block;
  margin-bottom: var(--space-sm);
}

.form-row select,
.assignment-card select {
  display: block;
  width: 100%;
  max-width: 300px;
  padding: var(--space-sm);
  background: var(--color-dominant);
  color: var(--color-text-primary);
  border: 1px solid var(--color-text-secondary);
  border-radius: 4px;
  font-size: 16px;
}

.form-row input[type="file"] {
  display: block;
  width: 100%;
  max-width: 400px;
  padding: var(--space-sm);
  background: var(--color-dominant);
  color: var(--color-text-primary);
  border: 1px solid var(--color-text-secondary);
  border-radius: 4px;
  font-size: 16px;
}
```
  </action>
  <verify>
    <automated>grep -q ".assignment-grid" static/css/main.css && grep -q ".assignment-card" static/css/main.css && grep -q ".form-row select" static/css/main.css</automated>
  </verify>
  <done>CSS includes assignment grid layout, card styling, and select/file input styling consistent with the existing design system.</done>
  <acceptance_criteria>
    - static/css/main.css contains `.assignment-grid {`
    - static/css/main.css contains `.assignment-card {`
    - static/css/main.css contains `.form-row select,`
    - static/css/main.css contains `.form-row input[type="file"] {`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Flask -> state.json | Round assignments cross from untrusted client to persisted disk state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | Round assignment POST | mitigate | Pydantic RoundPrepForm validates input; only string values accepted; no code execution path |
| T-04-06 | Denial of Service | Rapid round changes | accept | Local operator tool; no rate limiting required for single-user desktop use |
</threat_model>

<verification>
- `curl -s http://localhost:5002/round_prep | grep -q "Round Prep"` returns 0 (after app is running)
- `curl -s -X POST -d "direction=next" http://localhost:5002/change_round` returns `{"success":true,"round":2}`
</verification>

<success_criteria>
Operator can open /round_prep, see the current round and mode, use Previous/Next to navigate rounds, assign players to cabinets from dropdowns, and save assignments to disk.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tournament-setup-round-prep-ui/04-03-SUMMARY.md`
</output>

---
phase: 04-tournament-setup-round-prep-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state.py
  - src/config/loader.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - AUTO-05
must_haves:
  truths:
    - "RuntimeState persists mode, current_round, and cabinet_assignments to disk"
    - "Loading runtime state restores all new fields with correct defaults"
    - "Player names can be extracted from both team and individual configs"
  artifacts:
    - path: "src/state.py"
      provides: "Extended RuntimeState with mode, current_round, cabinet_assignments"
    - path: "src/config/loader.py"
      provides: "get_player_names helper"
  key_links:
    - from: "src/state.py"
      to: "runtime/state.json"
      via: "save_runtime_state writes asdict"
      pattern: "json.dump(asdict"
    - from: "src/config/loader.py"
      to: "src/config/models.py"
      via: "get_player_names reads TeamsConfig and IndividualScheduleConfig"
      pattern: "teams_config\\.teams|individual_config\\.groups"
---

<objective>
Extend RuntimeState with tournament mode, round number, and cabinet assignment fields, and add a helper to extract player names from loaded configs.

Purpose: Provide the data model and utility functions that the Config and Round Prep pages depend on.
Output: Updated src/state.py and src/config/loader.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-tournament-setup-round-prep-ui/04-CONTEXT.md
@.planning/phases/04-tournament-setup-round-prep-ui/04-PATTERNS.md
@src/state.py
@src/config/loader.py
@src/config/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend RuntimeState with tournament fields</name>
  <files>src/state.py</files>
  <read_first>src/state.py</read_first>
  <action>
Add three new fields to the RuntimeState dataclass and update load_runtime_state with fallback defaults:

1. Add `mode: str = ""` after `obs_connected: bool = False`
2. Add `current_round: int = 1` after `mode`
3. Add `cabinet_assignments: Dict[str, str] = field(default_factory=lambda: {"IIDX#1": "Unassigned", "IIDX#2": "Unassigned", "IIDX#3": "Unassigned", "IIDX#4": "Unassigned"})` after `current_round`

Update `load_runtime_state` to pass these fields from the JSON data:
- `mode=data.get("mode", "")`
- `current_round=data.get("current_round", 1)`
- `cabinet_assignments=data.get("cabinet_assignments", {"IIDX#1": "Unassigned", "IIDX#2": "Unassigned", "IIDX#3": "Unassigned", "IIDX#4": "Unassigned"})`

Ensure the `Dict` import already exists (it does in the current file).
  </action>
  <verify>
    <automated>python -c "from src.state import RuntimeState, load_runtime_state; s = RuntimeState(); assert s.mode == ''; assert s.current_round == 1; assert s.cabinet_assignments['IIDX#1'] == 'Unassigned'; rs = load_runtime_state(); assert hasattr(rs, 'mode')"</automated>
  </verify>
  <done>RuntimeState has mode, current_round, and cabinet_assignments with correct defaults; load_runtime_state restores them from disk.</done>
  <acceptance_criteria>
    - src/state.py contains the line `mode: str = ""`
    - src/state.py contains the line `current_round: int = 1`
    - src/state.py contains `cabinet_assignments: Dict[str, str] = field(default_factory=lambda: {"IIDX#1": "Unassigned", "IIDX#2": "Unassigned", "IIDX#3": "Unassigned", "IIDX#4": "Unassigned"})`
    - load_runtime_state contains `mode=data.get("mode", "")`
    - load_runtime_state contains `current_round=data.get("current_round", 1)`
    - load_runtime_state contains `cabinet_assignments=data.get("cabinet_assignments", {"IIDX#1": "Unassigned", "IIDX#2": "Unassigned", "IIDX#3": "Unassigned", "IIDX#4": "Unassigned"})`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add get_player_names helper to config loader</name>
  <files>src/config/loader.py</files>
  <read_first>src/config/loader.py, src/config/models.py</read_first>
  <action>
Add a `get_player_names` function to src/config/loader.py that extracts all player names from the already-loaded config dictionary based on tournament mode.

Add `List` to the typing imports if not already present.

Insert the following function after `load_configs`:

```python
def get_player_names(configs: Dict[str, Any], mode: str) -> List[str]:
    """Return a sorted list of unique player names for the given mode."""
    names: List[str] = []
    if mode == "team":
        teams_config = configs.get("teams")
        if teams_config:
            for team in teams_config.teams:
                for player in team.players:
                    names.append(player.name)
    elif mode == "individual":
        individual_config = configs.get("individual_schedule")
        if individual_config:
            for group in individual_config.groups.values():
                names.extend(group)
    return sorted(set(names))
```

This function must use the validated Pydantic objects returned by `load_configs()`, so attribute access like `teams_config.teams` is valid.
  </action>
  <verify>
    <automated>python -c "from src.config.loader import load_configs, get_player_names; configs = load_configs(); print(get_player_names(configs, 'team')); print(get_player_names(configs, 'individual'))"</automated>
  </verify>
  <done>get_player_names exists and returns sorted unique player names for both 'team' and 'individual' modes.</done>
  <acceptance_criteria>
    - src/config/loader.py contains `def get_player_names(configs: Dict[str, Any], mode: str) -> List[str]:`
    - Function body contains `mode == "team"`
    - Function body contains `mode == "individual"`
    - Function returns `sorted(set(names))`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file-system -> app | Uploaded configs are written to disk and parsed as JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Config upload | mitigate | Pydantic validation before write; backup existing file |
| T-04-02 | Denial of Service | JSON parsing | mitigate | json.load with standard limits; catch exceptions and return 200 with error message |
</threat_model>

<verification>
- `python -c "from src.state import RuntimeState; s = RuntimeState(); assert s.cabinet_assignments['IIDX#4'] == 'Unassigned'"` passes
- `python -c "from src.config.loader import get_player_names; import inspect; assert 'sorted(set(names))' in inspect.getsource(get_player_names)"` passes
</verification>

<success_criteria>
RuntimeState supports mode, current_round, and cabinet_assignments with disk persistence. Config loader can extract player names from team and individual configs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tournament-setup-round-prep-ui/04-01-SUMMARY.md`
</output>

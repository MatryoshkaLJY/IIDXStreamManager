---
phase: 02-tournament-rule-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - iidx_knockout_scoreboard/index.html
  - iidx_knockout_scoreboard/style.css
  - iidx_knockout_scoreboard/app.js
autonomous: true
requirements:
  - UI-01
  - SCOR-01
  - CMD-01
must_haves:
  truths:
    - "Champion trophy HTML and CSS are fully deleted, not just hidden"
    - "Init command updates only .title-text and leaves .stage-indicator intact"
    - "Score commands accumulate raw scores across rounds for groups A-F"
    - "Cumulative scores are displayed in player nodes during group stages"
  artifacts:
    - path: "iidx_knockout_scoreboard/index.html"
      provides: "Tournament tree DOM without champion trophy"
      lacks: "champion-trophy"
    - path: "iidx_knockout_scoreboard/style.css"
      provides: "Visual styling without trophy rules"
      lacks: ".champion-trophy"
    - path: "iidx_knockout_scoreboard/app.js"
      provides: "Fixed init handler and cumulative scoring logic"
      contains: ["handleInit", "cumulative", "totalScore"]
  key_links:
    - from: "app.js handleInit"
      to: "index.html .title-text"
      via: "textContent assignment"
      pattern: "querySelector\\(\\.title-text\\).*textContent"
    - from: "app.js handleScore"
      to: "player node score display"
      via: "cumulative raw score update"
      pattern: "score.*\\+.*=|totalScore"
---

<objective>
Clean up legacy champion trophy code, fix the init command to preserve header structure, and implement cumulative raw score tracking across group rounds.

Purpose: Removes dead code and establishes correct scoring foundation before progression and finals visuals.
Output: Updated `index.html`, `style.css`, and `app.js` with trophy removed, init fixed, and cumulative scoring working.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tournament-rule-refinements/02-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

## Source file status
The files `iidx_knockout_scoreboard/{app.js,index.html,style.css}` are the canonical targets. If they are not present in the working tree, locate the v1.0 knockout scoreboard source (likely in a prior branch or archive) and bring them into this path before editing.

## Specific implementation notes (from 02-CONTEXT.md)
- Champion trophy: delete the commented `<div class="champion-trophy">` block from `index.html` and remove all `.champion-trophy` CSS rules from `style.css`.
- Init fix: change `handleInit` to query `.title-text` directly and update only its `textContent`, rather than rebuilding `.tournament-title` innerHTML.
- Cumulative scoring: in `handleScore`, accumulate raw scores per player across rounds for groups A-F and render the running total in the player node.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove champion trophy and fix init command</name>
  <files>iidx_knockout_scoreboard/index.html, iidx_knockout_scoreboard/style.css, iidx_knockout_scoreboard/app.js</files>
  <action>
1. In `index.html`, fully delete any `&lt;div class="champion-trophy"&gt;` element (including commented-out versions). Do not leave CSS comments or hidden wrappers.
2. In `style.css`, remove all rulesets targeting `.champion-trophy` and its child selectors (e.g., `.champion-trophy img`, `.champion-trophy::after`).
3. In `app.js`, locate `handleInit`. Change it to select `.title-text` specifically and set `element.textContent = payload.title` (or equivalent). Ensure `.stage-indicator` inside `.tournament-title` is never overwritten.
  </action>
  <verify>
    <automated>grep -r "champion-trophy" iidx_knockout_scoreboard/index.html iidx_knockout_scoreboard/style.css && echo "FAIL: trophy remains" || echo "PASS: trophy removed"</automated>
    <automated>grep -n "innerHTML.*tournament-title\|innerHTML.*stage-indicator" iidx_knockout_scoreboard/app.js && echo "FAIL: init still mutates container" || echo "PASS: init safe"</automated>
  </verify>
  <done>Champion trophy code fully removed; `handleInit` only mutates `.title-text` text content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cumulative raw score tracking</name>
  <files>iidx_knockout_scoreboard/app.js</files>
  <behavior>
    - Given a player in group A with no prior scores, when handleScore receives {group:"A", index:0, score:950000}, the player's displayed raw score becomes 950000.
    - When a second score 980000 arrives for the same player, the displayed raw score becomes 1930000.
    - Given a player in finals, cumulative group scoring does not apply (finals uses per-round score display).
    - Cumulative scores must be rendered in the player node alongside points (PT).
  </behavior>
  <action>
1. Extend the player state object (or a parallel map) to track `totalScore` for each player.
2. In `handleScore`, if the current stage is a group stage (A-F), add the incoming raw score to the player's `totalScore`. If the stage is finals, store only the current round score (do not accumulate across final rounds unless it is a tiebreaker scenario handled in Plan 02).
3. Update `updatePlayerNode` (or equivalent renderer) to display the cumulative raw score for group stages. Keep the existing PT display unchanged.
4. Ensure `handleReset` clears cumulative scores.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const code = fs.readFileSync('iidx_knockout_scoreboard/app.js','utf8');
const hasTotal = /totalScore|total_score|cumulative/i.test(code);
const hasAdd = /\+\=.*score|score.*\+\=/i.test(code);
console.log(hasTotal && hasAdd ? 'PASS: cumulative scoring present' : 'FAIL: missing cumulative logic');
"</automated>
  </verify>
  <done>Group stage scores accumulate across rounds; player nodes show running total raw score; reset clears totals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket message -> app.js | Untrusted input from operator control panel crosses into UI state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | handleInit / handleScore | mitigate | Preserve DOM structure by targeting specific elements; validate numeric score inputs before arithmetic |
| T-02-02 | Denial of Service | handleScore large payload | accept | Local operator control; no external network exposure |
</threat_model>

<verification>
- `grep -i "champion-trophy"` across `index.html` and `style.css` returns zero results.
- `app.js` `handleInit` uses `querySelector('.title-text').textContent` and never touches `innerHTML` on the title container.
- Simulated `score` commands for the same player in group A add up and display the correct cumulative total.
</verification>

<success_criteria>
- Champion trophy code and UI fully removed (UI-01)
- Init command preserves `.stage-indicator` and only updates `.title-text` (CMD-01)
- Score commands accumulate raw scores across rounds for groups A-F (SCOR-01)
</success_criteria>

<output>
After completion, create `.planning/phases/02-tournament-rule-refinements/02-tournament-rule-refinements-01-SUMMARY.md`
</output>

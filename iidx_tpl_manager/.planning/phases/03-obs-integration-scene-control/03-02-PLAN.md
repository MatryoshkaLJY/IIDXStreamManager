---
phase: 03-obs-integration-scene-control
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app.py
  - src/obs/heartbeat.py
  - tests/test_app_obs_routes.py
  - tests/test_heartbeat.py
autonomous: true
requirements: [OBS-01, OBS-02, OBS-04]
user_setup: []

must_haves:
  truths:
    - Flask app initializes OBS client lazily without crashing on startup
    - Operator can POST to /switch_scene to change OBS scenes with server-side validation
    - Operator can POST to /obs_config to update connection settings with Pydantic validation
    - SocketIO obs_reconnect event triggers reconnection and scene re-validation
    - Background heartbeat emits obs_status events every 3 seconds without auto-reconnecting
  artifacts:
    - path: src/app.py
      provides: Flask routes /switch_scene and /obs_config, SocketIO handlers, lazy client initialization
    - path: src/obs/heartbeat.py
      provides: OBSHeartbeat daemon thread with 3-second interval
    - path: tests/test_app_obs_routes.py
      provides: Route and SocketIO handler tests
    - path: tests/test_heartbeat.py
      provides: Heartbeat thread behavior tests
  key_links:
    - from: src/obs/heartbeat.py
      to: src/app.py
      via: socketio.emit("obs_status", ...) pushed from background thread
    - from: src/app.py
      to: src/obs/scene_controller.py
      via: SceneController instance created during app initialization
    - from: src/app.py
      to: src/state.py
      via: load_runtime_state and save_runtime_state for OBS config persistence
---

<objective>
Integrate the OBS backend into the Flask application: add scene-switching and config routes, SocketIO event handlers for manual reconnect, a background heartbeat thread for connection health monitoring, and comprehensive tests.

Purpose: This plan wires the OBS client and scene controller into the actual web app so the operator can control OBS from the browser.
Output: Updated src/app.py, new heartbeat module, and tests for routes and heartbeat behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-obs-integration-scene-control/03-CONTEXT.md
@.planning/phases/03-obs-integration-scene-control/03-RESEARCH.md
@.planning/phases/03-obs-integration-scene-control/03-PATTERNS.md

# Existing app patterns
From src/app.py:
```python
def create_app(return_socketio: bool = False):
    app = Flask(__name__, template_folder="templates", static_folder="static")
    app.config["SECRET_KEY"] = os.environ.get("SECRET_KEY", "dev-secret-change-in-production")
    socketio = SocketIO(app, cors_allowed_origins="*")
    ...
    if return_socketio:
        return app, socketio
    return app
```

From 03-RESEARCH.md (Pattern 3 - Background Heartbeat Thread):
```python
class OBSHeartbeat:
    def __init__(self, obs_client, socketio: SocketIO, interval: float = 2.0):
        self.obs = obs_client
        self.socketio = socketio
        self.interval = interval
        self._stop = threading.Event()
        self._thread = None

    def start(self):
        if self._thread and self._thread.is_alive():
            return
        self._stop.clear()
        self._thread = threading.Thread(target=self._run, daemon=True)
        self._thread.start()

    def stop(self):
        self._stop.set()
        if self._thread:
            self._thread.join(timeout=self.interval + 1)

    def _run(self):
        while not self._stop.is_set():
            try:
                if self.obs.connected:
                    self.obs.get_version()
            except Exception:
                self.obs.connected = False
                try:
                    self.obs.disconnect()
                except Exception:
                    pass
            self.socketio.emit("obs_status", {"connected": self.obs.connected})
            self._stop.wait(self.interval)
```

Per D-09: NO auto-reconnect in the heartbeat. Reconnection is triggered only by the manual "Reconnect to OBS" button.
Per D-03/D-04: On startup, validate scene names; if missing, start anyway with warnings and disabled controls.
Per D-05: Operator can click "Retry validation" or "Reconnect" without restarting the app.
Per D-08: If disconnected, show warning banner with manual reconnect button.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create heartbeat module and integrate OBS into Flask app</name>
  <files>
    src/app.py
    src/obs/heartbeat.py
  </files>
  <read_first>
    src/app.py
    src/obs/client.py
    src/obs/scene_controller.py
    src/state.py
  </read_first>
  <action>
    1. Create `src/obs/heartbeat.py` with `OBSHeartbeat` class:
       - `__init__(self, obs_client, socketio, interval: float = 3.0)` stores references and creates `threading.Event()`
       - `start(self) -> None` starts a daemon thread running `_run` if not already alive
       - `stop(self) -> None` sets the stop event and joins the thread with timeout `interval + 1`
       - `_run(self) -> None` loop:
         * `while not self._stop.is_set():`
         * Try `self.obs.get_version()` only if `self.obs.connected` is True
         * On any exception: set `self.obs.connected = False`, then call `self.obs.disconnect()` (swallow exceptions)
         * Emit `self.socketio.emit("obs_status", {"connected": self.obs.connected})`
         * `self._stop.wait(self.interval)`
       Import `threading` and `flask_socketio.SocketIO`. Use type hints.

    2. Update `src/app.py`:
       - Add imports: `from pydantic import BaseModel, Field, validator` (or `field_validator` for Pydantic v2), `from src.obs import OBSClient, SceneController`, `from src.obs.heartbeat import OBSHeartbeat`
       - After `socketio = SocketIO(...)`, create an `OBSClient` instance using values from `load_runtime_state()` (do NOT call `.connect()` yet)
       - Create a `SceneController` instance wrapping the client
       - Define a helper `def _validate_and_emit_obs_state()` that:
         * calls `scene_controller.validate_scenes()`
         * saves `obs_connected` and `missing_scenes` state to `runtime_state`
         * calls `save_runtime_state(runtime_state)`
         * emits `socketio.emit("obs_status", {"connected": client.connected, "scenes_valid": scene_controller.scenes_valid, "missing_scenes": scene_controller.missing_scenes})`
       - In the eager config load block (inside `with app.app_context():`), after loading configs, load runtime state and set up OBS client params from it. Then attempt a guarded connection and validation:
         ```python
         try:
             client.connect()
             _validate_and_emit_obs_state()
         except Exception:
             client.connected = False
         ```
         Do NOT let this exception crash the app startup.
       - Add POST route `/switch_scene`:
         * Accepts JSON `{"scene": "现场摄像"}` (or form data with `scene` key)
         * Validates that `scene` is in `REQUIRED_SCENES.values()`
         * Calls `scene_controller.switch_to(scene)`
         * Returns JSON `{"success": True}` or `{"success": False, "error": "..."}` with HTTP 200
       - Add POST route `/obs_config`:
         * Accepts form data or JSON with `host`, `port`, `password`
         * Validate with a small Pydantic model `OBSConfigForm`:
           - `host: str` (non-empty)
           - `port: int = Field(..., ge=1, le=65535)`
           - `password: str = ""`
         * On validation error, return JSON `{"success": False, "error": "..."}`
         * On success, update `runtime_state` with new values, save to disk, update `client.host/port/password`, disconnect old connection if any, then attempt `client.connect()` and `_validate_and_emit_obs_state()`
         * Return JSON `{"success": True, "connected": client.connected, "scenes_valid": scene_controller.scenes_valid}`
       - Add SocketIO event handler `@socketio.on("obs_reconnect")`:
         * `def handle_obs_reconnect():`
         * Disconnect existing client if connected
         * Attempt `client.connect()`
         * Call `_validate_and_emit_obs_state()`
       - Start the heartbeat thread after app creation: `heartbeat = OBSHeartbeat(client, socketio, interval=3.0); heartbeat.start()`
       - Ensure heartbeat is stopped gracefully if possible (Flask does not have a standard shutdown hook; document this in a code comment)
       - Update `status()` route to pass `obs_connected`, `scenes_valid`, `missing_scenes`, and `runtime_state.obs_host`/`obs_port`/`obs_password` to the template
  </action>
  <verify>
    <automated>python -c "from src.app import create_app; app = create_app(); print('app creates ok')"</automated>
  </verify>
  <done>
    - src/obs/heartbeat.py exists with OBSHeartbeat class, 3-second interval, no auto-reconnect
    - src/app.py creates OBSClient and SceneController without crashing on startup
    - src/app.py has POST /switch_scene and POST /obs_config routes
    - src/app.py has SocketIO handler for obs_reconnect
    - Heartbeat thread is started in create_app
  </done>
  <acceptance_criteria>
    - `grep -n "class OBSHeartbeat" src/obs/heartbeat.py` returns exactly one match
    - `grep -n "def _run" src/obs/heartbeat.py` returns exactly one match
    - `grep -n "socketio.emit.*obs_status" src/obs/heartbeat.py` returns at least one match
    - `grep -n "def _run" src/obs/heartbeat.py` and `grep -n "connect()" src/obs/heartbeat.py` do NOT appear together (no auto-reconnect in heartbeat)
    - `grep -n "/switch_scene" src/app.py` returns at least one match for the route decorator
    - `grep -n "/obs_config" src/app.py` returns at least one match for the route decorator
    - `grep -n "obs_reconnect" src/app.py` returns at least one match for the SocketIO handler
    - `grep -n "OBSHeartbeat" src/app.py` returns at least one match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write tests for app routes and heartbeat</name>
  <files>
    tests/test_app_obs_routes.py
    tests/test_heartbeat.py
  </files>
  <read_first>
    tests/test_state.py
    src/app.py
    src/obs/heartbeat.py
  </read_first>
  <action>
    1. Create `tests/test_app_obs_routes.py`:
       - `test_create_app_does_not_crash_when_obs_unavailable()` — patch `obsws_python.ReqClient` to raise `ConnectionRefusedError` and assert `create_app()` still returns an app
       - `test_switch_scene_returns_error_when_obs_not_ready()` — use Flask test client to POST `{"scene": "现场摄像"}` and assert `success: False`
       - `test_switch_scene_succeeds_when_obs_ready()` — patch client/scene_controller so `switch_to` returns `(True, "")`, POST and assert `success: True`
       - `test_switch_scene_rejects_unknown_scene()` — POST `{"scene": "InvalidScene"}` and assert `success: False`
       - `test_obs_config_validates_input()` — POST invalid port (e.g., 99999) and assert `success: False`
       - `test_obs_config_updates_state_and_attempts_connection()` — POST valid config and assert runtime state is updated (check file or mock save)
       - `test_socketio_obs_reconnect_handler_emits_status()` — use `socketio.test_client(app)` to emit `obs_reconnect` and verify `obs_status` is received in `get_received()`

    2. Create `tests/test_heartbeat.py`:
       - `test_heartbeat_starts_and_stops_without_crash()` — create with mock obs_client and a real SocketIO from a test app, start/stop
       - `test_heartbeat_emits_status_on_each_tick()` — mock `obs.connected = True`, start heartbeat, wait slightly, stop, and verify emit was called
       - `test_heartbeat_sets_connected_false_on_exception()` — mock `obs.get_version` to raise `RuntimeError`, start heartbeat briefly, stop, assert `obs.connected` was set to False and emit was called with `connected: False`
       - `test_heartbeat_does_not_auto_reconnect()` — mock `obs.connected = False`, verify `obs.connect()` is never called during the heartbeat run
       Use `unittest.mock.Mock` for `obs_client` and `socketio`. For socketio mocking in heartbeat tests, you can pass a `Mock()` as socketio and assert `emit` calls, or use a minimal Flask app with SocketIO and the test client.
  </action>
  <verify>
    <automated>pytest tests/test_app_obs_routes.py tests/test_heartbeat.py -v</automated>
  </verify>
  <done>
    - tests/test_app_obs_routes.py covers startup resilience, scene switching, config validation, and SocketIO reconnect
    - tests/test_heartbeat.py covers start/stop, emission, disconnect detection, and no-auto-reconnect
    - All tests pass
  </done>
  <acceptance_criteria>
    - `pytest tests/test_app_obs_routes.py -x` exits with code 0
    - `pytest tests/test_heartbeat.py -x` exits with code 0
    - `tests/test_app_obs_routes.py` contains `test_create_app_does_not_crash_when_obs_unavailable`
    - `tests/test_heartbeat.py` contains `test_heartbeat_does_not_auto_reconnect`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Flask POST /obs_config | Untrusted user input crosses into server config |
| Heartbeat thread -> SocketIO emit | Server-generated state crosses to browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | OBSHeartbeat._run | mitigate | Hardcode interval to 3.0 seconds; use threading.Event.wait() to avoid tight loops |
| T-03-04 | Tampering | POST /obs_config | mitigate | Pydantic validation: host non-empty string, port clamped to 1-65535; reject malformed input |
| T-03-05 | Information Disclosure | POST /obs_config response | mitigate | Do not echo password back in the JSON response |
</threat_model>

<verification>
- `pytest tests/test_app_obs_routes.py tests/test_heartbeat.py -v` passes
- `python -c "from src.app import create_app; app = create_app()"` succeeds even when OBS is not running
- No auto-reconnect logic exists in `src/obs/heartbeat.py`
</verification>

<success_criteria>
- Flask app starts successfully regardless of OBS availability
- /switch_scene validates scene names server-side and returns structured JSON
- /obs_config validates inputs with Pydantic and persists to runtime state
- SocketIO obs_reconnect handler triggers connection and validation
- Heartbeat emits obs_status every 3 seconds and does NOT auto-reconnect
</success_criteria>

<output>
After completion, create `.planning/phases/03-obs-integration-scene-control/03-02-SUMMARY.md`
</output>
